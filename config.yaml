name: "My Example Server"
description: "An example MCP server"
icon: "assets/icon.png"

auth:
  type: cred
  credentials:
    - name: "api_key"
      label: "API Key"
      description: "Your API key"
      placeholder: "sk-..."
      secret: true